services:
  - type: web
    name: progenx-api
    runtime: python
    plan: free
    buildCommand: "cd backend && pip install -r requirements.txt"
    startCommand: "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: EVO2_NVIDIA_API_KEY
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        value: "https://progenx.ai"
      - key: DATABASE_URL
        value: "sqlite:///./progenx.db"
    disk:
      name: progenx-data
      mountPath: /app/backend
      sizeGB: 1
